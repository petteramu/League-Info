import { moduleFor, test } from 'ember-qunit';

moduleFor('view:player-item', 'Unit | View | player item');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
