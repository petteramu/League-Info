import { msToTime } from '../../../helpers/ms-to-time';
import { module, test } from 'qunit';

module('Unit | Helper | ms to time');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = msToTime(42);
  assert.ok(result);
});
